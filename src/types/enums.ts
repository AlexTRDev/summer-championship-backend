export enum UserStatus {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
}

export enum SerieTeam {
  A = 'A',
  B = 'B',
  C = 'C',
}

export enum Genre {
  MASCULINO = 'M',
  FEMENINO = 'F',
  OTRO = 'O',
}
