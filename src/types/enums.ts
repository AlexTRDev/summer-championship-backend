export enum Status {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  OPERATOR = 'OPERATOR',
  CLIENT = 'CLIENT',
}

export enum SerieTeam {
  A = 'A',
  B = 'B',
  C = 'C',
}

export enum Gender {
  MALE = 'M',
  FEMALE = 'F',
  OTHER = 'O',
}

export enum Result {
  HOME = 'V',
  AWAY = 'L',
  DRAW = 'E',
}
